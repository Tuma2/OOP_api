services:
  - type: web
    name: oop-learning-api
    env: python
    plan: free
    pythonVersion: 3.11
    buildCommand: "pip install --no-cache-dir -r requirements.txt"
    startCommand: "uvicorn main:app --host 0.0.0.0 --port $PORT"
    envVars:
      - key: PYTHON_VERSION
        value: 3.11